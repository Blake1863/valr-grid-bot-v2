# 2026-08-19

## 🛑 Wash-Trading Wind-Down — COMPLETE (Blake's call, 22:44)
All wash trading (spot + futures) terminated.
- **Services stopped + disabled:** `valr-cm-spot@{zar,usdt,usdc,btc}.service`, `cm-bot-v2.service`
- **Cron removed:** wash-bot-prints-check, wash-bot-inventory-restock-check, tbgs-v3-finalize, jess photo job. Cron list now empty.
- **Futures:** all 12 positions on CM1/CM2 closed (reduce-only market orders). CM2's 1-XRP short was below the 2-XRP min order size — padded to 2 then closed.
- **Spot:** all subs liquidated (base→quote→USDT via USDCZAR/USDTZAR), balances swept to MAIN.
- **MAIN final: ~528.43 USDT.** Dust left: 0.90 ZAR, 0.027 USDC, 0.21 EURC, 0.0005 VALR10, 0.0023 BITGOLD (+ sub base-dust below min order sizes, ZARSC staking credits).
- Balance-report bot kept (read-only). Joint-savings bot unaffected (still paused).
- MEMORY.md updated; do-not-restart note added.

## Gotchas learned tonight
- `/v1/account/subaccounts/transfer` must be signed with EMPTY subaccount id; signing with the sub id → 401.
- Futures close = opposite-side market order + `reduceOnly:true`.
- Position below min order size: pad to min, then close.
- MAIN balances read with NO subaccount header (header "0" returns empty).

## OpenClaw instability tonight
- Host DNS kept dropping → model calls hung → watchdog auto-restarted gateway ~17x. That killed long-running jobs repeatedly (incl. video render attempts).
- Session fell back to opus/gpt-5.5; reset to modelstudio/qwen3.8-max (the configured primary).
- Config verified consistent; `openclaw status` clean.

## Hardening deployed (23:40-ish, Blake-approved)
1. **Smarter watchdog**: dead unit → restart after 3 fails; alive-but-hung → 10 fails (~5 min grace). Backup: `oc-watchdog.py.bak-20260819`.
2. **Model fast-fail**: `models.providers.modelstudio.timeoutSeconds: 90` — hot-reloaded OK (config backup: `openclaw.json.bak-20260819`).
3. **Remote bot /doctor**: runs `openclaw doctor --fix --non-interactive`; tested locally — "Doctor complete", 0 errors.
- Updated TOOLS.md + AGENTS.md (resilience conventions: detach long jobs, idempotent/checkpointed, dry-run money moves).

## Video project (TBGS) — CLOSED per Blake: "no more videos"
- Last request: strip background audio under the dubbed shots. Remix ran VO-only but final render kept getting killed by gateway restarts; final clean cut was NOT delivered before Blake closed the project. Do not resume video work.

## Open items
- Wash-bot Telegram alerter was misconfigured (missing token/chat_id) — moot now that bots are stopped.
- `btc` spot bucket had been running empty (no inventory) before wind-down.
