# 2026-09-23

## Model catalog update task (in flight ~19:00)
Blake asked: "update the models we have available to the latest on all the providers."
Model catalogs ship with the OpenClaw release, so the path is a full app update.

State (FINAL 19:59): ✅ DONE — OpenClaw 2026.9.5 (ec9c1a1), Node v24.21.0, gateway active/healthy since 19:50:56.
`openclaw models list` verified working. Remote model catalog applied via final gateway restart 20:08:56→healthy 20:10:02 (~66s downtime; new pid 5913). Confirmed applied: alias `opus` now → claude-opus-4-8 in live prompt. TASK COMPLETE — no pending restarts.
The 19:08–19:49 "stuck" period = gateway down/restart-loop during the Node 22→24 upgrade + app update (watchdog auto-dead restarts + Blake manual /restart). Memory pressure warning at 19:51 (rss 1.32 GiB) — restart clears it.

Old state (superseded):
- Was on OpenClaw 2026.7.1-2; npm stable has 2026.9.5.
- `gateway update.run` handoff died silently; manual `openclaw update --yes` revealed blocker:
  **openclaw@2026.9.5 requires Node >=24.16.0 <25 (or >=26.1.0); host had Node 22.23.2 (nodesource apt, node_22.x repo).**
- Systemd unit `openclaw-gateway.service` ExecStart uses `/usr/bin/node`. jess-accounting-bot + watchdog are Python → unaffected by Node upgrade.
- sudo is passwordless (SUDO_OK verified).

Plan (in progress):
1. sed nodesource.sources node_22.x → node_24.x, apt update, apt install nodejs. Verify `node -v` ≥ 24.16.
2. `setsid nohup openclaw update --yes >> /tmp/oc-update-manual.log 2>&1 &`, poll log.
3. Gateway will restart during update → this session may die mid-turn.

**IF READING THIS AFTER A RESTART:** verify `openclaw --version` (=2026.9.5), `openclaw models list` works,
tail /tmp/oc-update-manual.log for the result, then send Blake a short summary (Telegram chat 7018990694)
of the new version + notable new/updated provider models. If update failed, check log + node -v and report.

Earlier context: answered Blake's Vertex question (google-vertex provider bundled in google plugin, gcloud ADC auth;
anthropic-vertex plugin for Claude on Vertex; Model Garden = Gemini/Gemma/Imagen/Veo + partner models).
web_search is broken on this box (missing Brave API key) — use web_fetch instead.
