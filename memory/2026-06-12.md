## 02:52 CST Jess bot image-capture diagnostics

- Investigated report: "There was an error capturing an image on the Jess bot."
- Service was not crashed or dead:
  - `jess-accounting-bot.service` was still `active (running)` from `2026-06-11 05:08:08 CST`
  - recent audit log entries showed successful writes on `2026-06-11`, so this did not look like a persistent OCR outage
- Existing stdout/stderr logs were effectively empty for upload failures, so there was not enough telemetry to identify whether the problem was Telegram file download, file validation, or OCR.
- Added proper logging to `bots/jess-accounting-bot`:
  - `bot/main.py` now configures Python logging
  - `bot/telegram_handlers.py` now logs receipt uploads and successful processing
  - Telegram image download failures now raise a clearer user-facing message: `I couldn't download that image from Telegram. Please send it again as a photo or JPG/PNG/PDF.`
  - unhandled exceptions now log full stack traces via `logger.exception(...)`
- Validation:
  - full Jess bot suite passes: `39 passed`
  - service restarted successfully at `2026-06-12 02:52:36 CST`
- Next useful action if the problem recurs: retry one image upload now that logging exists; the next failure should be diagnosable from the logs instead of silent.

## 19:20 CST Jess bot audit scoping fix

- Reviewed `bots/jess-accounting-bot` for correctness after the OCR/image-capture work.
- Found a cross-user safety bug: audit history, export bookkeeping, and `/undo` were using the global latest audit row rather than the current Telegram user.
- Fixed the controller/audit path so recent history and undo are scoped by `telegram_user_id`, and exports tag the session's own last completed audit row instead of whichever write happened most recently.
- Added regression coverage for:
  - per-user recent history
  - per-user export bookkeeping
  - per-user undo candidate selection
- Validation: Jess bot venv test suite now passes `42 passed`.

## 21:05 CST Jess bot multi-upload queue

- Implemented per-user receipt queueing in `bots/jess-accounting-bot` so multiple uploaded images/PDFs no longer overwrite the single `pending` slot.
- New behavior:
  - first upload becomes the active preview
  - later uploads are queued automatically
  - after confirm or cancel, the next queued receipt is shown automatically
- Added controller regression tests for queueing, confirm auto-advance, and cancel auto-advance.
- Validation: Jess bot venv test suite passes `45 passed`.

## 21:15 CST heartbeat

- Re-ran `HEARTBEAT.md` checks.
- Log sanity is fine:
  - no `*.log` files over `1G`
  - `cm-bot-logrotate.timer` is `active`
  - last rotation was `2026-06-12 21:02:30 CST`
  - quarantine is `4.0K`
- Futures wash bot is healthy:
  - `cm-bot-v2.service` is `active`
  - last 50 log lines contain `0` `ERROR|panic` matches
- Split wash services are running, but all three latest 5-minute summary buckets are below the configured throughput floor of `TOTAL prints all pairs >= 200`:
  - ZAR: `56`
  - USDT: `131`
  - USDC: `24`
  - no `ERROR|Exception|Traceback`, no `fail`/`incl` spikes, and all `internal_ratio` values seen were `1.00`
- Grid bot v4 still needs attention:
  - `valr-perpetual-grid-bot@sol.service` and `@eth.service` report `is-active`, but systemd shows both in `activating` / `auto-restart`
  - both have `Result=exit-code`, `ExecMainStatus=1`, `NRestarts=138`
  - latest failures are still sizing aborts:
    - SOL: `Computed qty 0 below minimum 0.01. Reduce gridCount or increase capital.`
    - ETH: `Computed qty 0 below minimum 0.001. Reduce gridCount or increase capital.`

## 23:45 CST heartbeat refresh

- Re-checked the same `HEARTBEAT.md` items against the later runtime clock.
- Log sanity still fine:
  - no `*.log` files over `1G`
  - `cm-bot-logrotate.timer` still `active`
  - last rotation now `2026-06-12 23:14:59 CST`
  - quarantine still `4.0K`
- Grid bot v4 still unhealthy:
  - both `valr-perpetual-grid-bot@sol.service` and `@eth.service` are `activating` with `SubState=auto-restart`
  - both now show `NRestarts=306`, `Result=exit-code`, `ExecMainStatus=1`
  - recent log tails still show the same sizing aborts and no stop-loss / circuit-breaker / insufficient-balance events
- Split wash services and `cm-bot-v2.service` are all `active`, with no `ERROR|Exception|Traceback|panic`, and deprecated legacy services remain `inactive`.
- Latest 5-minute throughput totals:
  - ZAR: `120` (still below the `200` alert floor)
  - USDT: `296` (recovered above the floor)
  - USDC: `52` (still below the `200` alert floor)
