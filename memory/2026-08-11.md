
## VALR bot housekeeping (evening)
- Removed `bots/valr-auto-sweep/` entirely (moved to .trash/2026-08-11/). Was never deployed; joint-savings-bot superseded it.
- Paused joint-savings-bot.service (stopped + disabled, unit/state kept). Resume: `systemctl --user enable --now joint-savings-bot`
- Security: migrated its VALR creds from plaintext config.json into encrypted vault as `joint_savings_api_key`/`joint_savings_api_secret`; patched src/main.py to load from vault; scrubbed config.json; perms 600/700.
- Creds had been plaintext with 664 perms since May 26; Blake accepted risk, no rotation for now.
