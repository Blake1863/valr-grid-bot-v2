
## ZARSC added to chart maintenance (20:18 GMT+8)
- Blake asked to add ZARSC/ZAR + ZARSC/USDT to wash-bot chart maintenance.
- Added `ZARSCZAR` to `config-zar.json`, `ZARSCUSDT` to `config-usdt.json` (both default print $1.50–4.00, 20s cycle). No code changes needed — pair_meta parses from public/pairs.
- Pair facts: pegged ~1.0 ZAR / ~0.0605 USDT. tickSize ZARSCZAR=0.0001, ZARSCUSDT=0.00001; min_base 8–10, min_quote 10/0.5; baseDecimalPlaces=0.
- Self-seeded via HEAL-BASE on both subs (~R294+R74 ZAR, 2×$17.50 USDT); prints internal on cycles 2–3.
- Restarted valr-cm-spot@{zar,usdt}. Note: restart briefly cancels all bucket orders (cancel_all_on_startup) — expected blip.
