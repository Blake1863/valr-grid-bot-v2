# 2026-09-24

## Task: latest models (Opus 5.5 / GPT-6) — DONE, now on 2026.9.6 officially
Morning: verified via provider docs that Anthropic current = Opus 5.5 / Fable 5.1 / Sonnet 5 / Haiku 4.5, OpenAI current = GPT-6 Astra / Sol / Luna. OpenClaw 2026.9.6 (npm latest, published 09-23 23:07Z) adds official support. Node v24.21.0 satisfies engines.

## Incident #1 (09:38–09:54): first update attempt → rolled back
- `gateway update.run` → handoff `openclaw update --yes`: staging ✓ canary ✓ install ✓ checks ✓ activation ✓, then post-activation `doctor --lint` (2026.9.6 code) CRASHED: "RangeError: Maximum call stack size exceeded" in update-history reconciliation + config-promotion authority-check (keys meta/models/wizard). Updater auto-rolled back to 2026.9.5 (verified). Downtime ~8min.
- My exec at 09:46:04 (status check) was SIGTERM'd mid-stop → service-child cleanup hung → shutdown failed exit 1 → systemd fought updater orchestration → prolonged chaos. RULE: never exec during swap windows.
- Evidence preserved: `memory/reference/update-fail-2026.9.6-handoff.log`. GitHub bug family: #153674, #153430 (unguarded recursion in config-migration code). Our exact variant unreported.
- Stopgap 10:29: added 6 models manually to config (canonical metadata npm-packed from 2026.9.6 tarball, cost ×1e-6 to $/token, no tieredPricing), moved aliases opus→claude-opus-5-5, GPT→gpt-6-astra, fable→claude-fable-5-1, sonnet→claude-sonnet-5; modelPolicy.allow += 6. Backup: `~/.openclaw/openclaw.json.bak-20260924-models`. Hot reload confirmed 10:29:31.

## Incident #2 (11:00–13:22): Blake's /update → 2h14m outage → SUCCEEDED via roll-forward
- 11:00:49 update.run #2 (Blake sent /update per my suggestion). 11:08:18 clean stop for swap.
- **11:09:30 WATCHDOG auto-dead restart fired MID-SWAP** ← root cause. Premature boot ran pre-startup DB migration (schema 17→18; backup `~/.openclaw/state/openclaw.sqlite.pre-startup-migration-*.bak` 11:10), then crashed ERR_MODULE_NOT_FOUND (half-swapped tree).
- Updater rolled package back to 2026.9.5, but DB now schema 18 → every boot refused: exit 78/CONFIG (RestartPreventExitStatus=78 → systemd auto-restart disabled too).
- 11:10–13:15 dead loop: watchdog restarted every 5min (~25×), all exit 78; each restart also pushed a "🔄 restarted" bot notice to Blake (spam). Blake fought via remote bot (/status /restart /doctor /doctorfix 11:12–11:16, /doctor 13:03) — same wall.
- ~13:16–13:22 managed handoff recovery ROLLED FORWARD: reinstalled 2026.9.6 to match schema-18 DB → clean boot 13:22:37. Codex plugin deferred migration completed on boot.
- Run record (via `openclaw update status`): run #2 ALSO failed at `openclaw doctor` step with the IDENTICAL config-promotion stack overflow → **their doctor bug confirmed twice**; final verification "gateway booted; service running; version verified".
- `openclaw update status` also warns: unit TimeoutStopSec differs from installer value **330** (ours: 30), KillMode differs from installer value **mixed** (ours: control-group), Description still says v2026.7.1-2 → stale unit metadata; installer-default 330s stop timeout would have given swap windows slack.
- Verified 13:24–13:41: version 2026.9.6 (eb377ac), npm tree complete, healthz live, all 6 models + aliases correct, DB backups intact.

## Watchdog hardening (DEPLOYED 13:40:44, active)
`~/openclaw-watchdog/oc-watchdog.py` (+7 surgical edits, py_compile OK, service restarted):
1. `update_in_flight()`: pgrep "openclaw-update-run-handoff|openclaw update" + fresh-mtime markers (/tmp/openclaw-update-run-handoff-*, .openclaw.update-stage-*, .openclaw.package-backup-*, age < OCW_UPDATE_HOLD_MAX_AGE=1800s) → HOLD restarts, notify once/10min.
2. Crash-loop backoff: last ExecMainStatus == OCW_CONFIG_REFUSAL_EXIT (78) OR failed_restarts >= OCW_CRASHLOOP_THRESHOLD (3, restarts that never reached healthy) → one 🚨 alert + retry at most once per OCW_CRASHLOOP_BACKOFF (1800s) window. Was: restart+push every 300s forever.
3. Healthy branch resets failed_restarts/backoff/notified. Startup log shows: `update-hold=1800s crashloop=3x/1800s refusal-exit=78`.

## Post-update health (2026.9.6)
- `openclaw doctor --non-interactive`: EXIT 0 CLEAN (morning's update-gate crash did NOT reproduce in direct context).
- Pending (Blake's call): plugin drift brave+qwen 2026.7.1 → `openclaw plugins update @openclaw/brave-plugin@2026.9.6` + `@openclaw/qwen-provider@2026.9.6`, then gateway restart (~1-2min). Plugin registry "stale non-authoritative generations". github plugin notice (silence via plugins.entries.github.enabled=false). memory.search.rememberAcrossConversations notice.
- Systemd unit stale metadata REPAIRED manually (13:4x): TimeoutStopSec 30→330, KillMode control-group→mixed (installer values), daemon-reload done, gateway active+healthy after. Backups: `memory/reference/gateway-unit-pre-fix-20260924.service` + `/tmp/unit.pre-sed.bak`. Description left stale (cosmetic only).
- `openclaw doctor --fix` REFUSES via my exec: "running inside the gateway process tree (PID ...)" — must run outside the gateway: remote-control bot /doctor (runs under watchdog = outside) or a terminal. Plain `doctor --non-interactive` (no --fix) exits 0 clean. Config JSON verified intact after the refused --fix.
- **2026.9.6 BUG (mine-side tooling): `message(action=send)` returns "Tool message not found"** — failed 2× at ~13:2x/13:3x post-update. Workaround: turn-final text auto-routes to Telegram fine. Watch for recurrence; candidate bug report.

## RULES (hard-won today)
1. Never exec anything during an update swap window; rely on managed notices.
2. Watchdog must hold during updates (now enforced in code).
3. Exit 78 = config/schema refusal → restarts CANNOT fix; align package version to DB schema or restore pre-migration DB backup.
4. message() tool broken on 2026.9.6 → use final-reply routing.

## Workspace cleanup + storage reclamation (Blake ~14:52)
**Disk: 76% → ~51% (~10G freed).** RAM healthy (1.4G avail, swap untouched).
- Caches: npm cache 3.1G→26M; journald vacuumed −3.3G (cap 200M); apt cache 150M→32K
- Deleted stale logs: memory/ws-mm.log (195M, wash-bot, root-owned) + 9 empty memory/*.log + bots/cm-bot-spot-py/logs (1.5G)
- Deleted superseded codex npm build generation (Sep 23, 342M) in ~/.openclaw/npm/projects
- Git: committed April quarantine purge (7555 deletions); untracked bots/cm-bot-v2/target (745M removed from disk+index) and embedded skills/valr-exchange repo; .gitignore now covers .secrets/ .trash/ backups/ movies/ media/ .config/ .openclaw/ __pycache__/ bots/*/target/ *.migrated.*; verified NO secrets committed (.env.example placeholders only; brave key in .secrets/ stays untracked)
- Push had been silently failing since April: 202MB gz blob in .log-quarantine exceeded GitHub 100MB limit → range-limited filter-branch stripped .log-quarantine + target/ from the 38 unpushed commits (tree verified identical via backup tag), pushed clean, tag dropped, gc --prune=now
- Failed-update record: ABANDONED per Blake. Running npm latest 2026.9.6 healthy; no updater processes; record is historical, clears on next successful update. Repaired unit drift left by it: StartLimitBurst 5→10, StartLimitIntervalSec 60→300 (backup: backups/openclaw-gateway.service.bak-20260924, applies next restart)
- KEPT (deliberately): ~/Backups 2.4G (today's fresh openclaw backup), plugin-captures 1.5G (in use by running gateway; recreates on restart), session-sqlite-import-archive 182M (Sep 23 migration — purge candidate after ~Oct 7 if all stable), ~/.rustup 1.4G (valr-futures-grid-bot is Rust), codex-home logs 86M (live plugin state)
- OPEN: movies/ 995M on disk (git-ignored) — awaiting Blake's call on deletion
- 15:50 Blake confirmed: movies/ (995M) deleted — disk now 50% used
