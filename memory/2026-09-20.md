# 2026-09-20

## Security audit + hardening (requested by Blake ~04:12)

**Audit result: no unauthorized access found.**
- No successful SSH logins from unknown IPs; all historical logins = admin from 100.104.17.x (Tailscale).
- OpenClaw gateway loopback-only (18789). `openclaw security audit`: 0 critical, 4 known warns (exec security=full for main, autoAllowSkills, unpinned codex plugin, trustedProxies — moot while local-only).
- ufw active: only 22/80/443. No rogue users (only root+admin have shells), sudoers.d empty, crontab empty, timers = cm-bot-logrotate + launchpadlib cache only.
- Constant SSH brute-force from random IPs targeting root/admin/goldstonesventures (username scraped from hosted site).

**Changes applied (Blake approved "Yes"):**
1. SSH hardened via drop-in `/etc/ssh/sshd_config.d/60-hardening.conf`:
   - PasswordAuthentication no, KbdInteractiveAuthentication no, PermitRootLogin prohibit-password
   - Backup: `/etc/ssh/sshd_config.bak-20260920`. Rollback: delete drop-in + `systemctl restart ssh`.
   - Verified: local password-auth attempt → "Permission denied (publickey)".
2. fail2ban installed + enabled; jail.local: sshd, maxretry 5, bantime 1h, findtime 10m. Backup of jail.local if existed: jail.local.bak-20260920. On start it retro-banned 1046 IPs from journal history.
3. nginx audited: two legit sites —
   - `goldstonesventures.com` (static /var/www/goldstones + /api/ → 127.0.0.1:5050 python app, loopback-only) — has catch-all `_` server_name, so bare IP serves this site (explains attacker username choice). Let's Encrypt via certbot.
   - `knoopendraai.co.za` (static, HSTS on). Let's Encrypt.
   - Verdict: both look intentional → keep 80/443 open. Optional: drop `_` catch-all.

**Open items:**
- Blake to confirm the 2 authorized_keys are his: `swas-imported-key` (ECDSA, Aliyun SWAS import) + `opencode@sas-london` (ed25519). Remove unknowns.
- Blake to test one SSH login from his laptop (key-based) to confirm no lockout. Break-glass: Aliyun SWAS console/VNC if ever locked out.
- authorized_keys has one blank/malformed line (line 2, empty) — harmless, can tidy.

## Secrets audit + vault migration (Blake ~04:22, completed ~04:35)

**Scan results:**
- All secret-bearing files already chmod 600/700, admin-only: openclaw.json, ~/.openclaw/.env, gateway.systemd.env, watchdog.env, jess-accounting-bot/.env, ~/.ssh, vault .key, letsencrypt live/archive (700).
- No hardcoded API keys in bot scripts, goldstones live server.py, or wedding-weather.py (all read from env/vault).
- Git (GitHub remote Blake1863/valr-grid-bot-v2): NO secret files ever committed; jess .env gitignored; content pattern scan clean over recent history (full 3657-commit scan too slow, killed — filename scan was full-history clean, low residual risk).
- Encrypted vault already exists + in use: `~/.openclaw/secrets/` (Fernet/AES at rest, secrets.py CLI). Consumers: wedding-weather.py, test_secrets.rs, valr-futures-grid-bot.

**Actions taken:**
1. Migrated `cm_secrets.env` (VALR CM1/CM2 keys + OPENAI key) → vault entries `cm1_api_key`, `cm1_api_secret`, `cm2_api_key`, `cm2_api_secret`, `openai_api_key_cm`. Hash-verified round-trip (ALL-VERIFIED).
2. Patched `scripts/oai_asr.py` to fetch key from vault (py_compile + live vault-fetch tested OK).
3. `shred -u cm_secrets.env` — plaintext gone.
4. Dangling refs to cm_secrets.env remain in DEAD code only: chart-maintenance-rust/src/main.rs, cm-bot-v2/scripts/deploy.sh (stopped/disabled, wash trading never resurrects — acceptable).

**Must-stay-plaintext (active services, systemd EnvironmentFile/dotenv pattern, all 600):** gateway.systemd.env (model keys), ~/.openclaw/.env (Alibaba AK), openclaw.json (gateway config+tokens), watchdog.env (remote bot token), jess .env (its OPENAI key is DIFFERENT from cm's — hash-verified, no rotation cross-impact).

**Disk:** vda3 plain ext4, no LUKS — full-disk encryption not retrofittable on this VPS. Mitigations: 600 perms + SSH hardening + fail2ban.

**Recommended (needs Blake):** revoke VALR CM1/CM2 API keys (bots dead permanently — dead keys are safest); optional rotate openai_api_key_cm (sat plaintext at 600, low risk).
