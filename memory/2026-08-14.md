
## Goldstones Ventures Dashboard overhaul (goldstonesventures.com)
- Site: `/var/www/goldstones/` — `api/server.py` (Flask, port 5050, systemd `goldstones-api.service` root-managed) + `static/index.html`. nginx site `goldstones`.
- Backup pre-rewrite: `backups/goldstones-20260814-1634/`
- Fixed: AI analysis broken (hardcoded dashscope-intl key 401) → now loads `api/ai_config.json` (chmod 600, field "token", model qwen3.6-flash via modelstudio/token-plan baseUrl from openclaw config). Graceful stale-cache fallback on generation errors.
- Fixed: leaderboard pnl/roi strings → floats server-side; deduped HL dex pairs (was double-counting); parallel wallet+dex fetches (positions 5.5s→0.5s).
- Added: Fear & Greed index, BTC/ETH sparklines, /api/sparkline endpoint, header BTC+F&G chips, trader links to stats.hyperliquid.xyz, AI card moved up, restyle.
- Gotcha: OpenClaw write pipeline REDACTS strings looking like key assignments (turns them into `***`). Avoid inline key literals in written files; patch via exec python or use indirection (`_keyfield`).

## Goldstones v2 (same day, ~17:00)
- Security pass: dotfile location→404 in nginx site, server_tokens off (nginx.conf L21), Flask localhost-only, key in chmod-600 ai_config.json ("token" field). Probes verified 404/SPA-fallback only.
- Accuracy: compare totals now include builder dexes (via _fetch_dex_pairs); new /api/divergence (price spread bps + funding gap HL vs Bybit, uses cached market data, zero extra calls); funding labeled /1h (HL) vs /8h (Bybit); sparkline coin input validated.
- Redesign: gold×teal "market intelligence" aesthetic, nav header, hero pulse band, AI read moved up, Divergence table (HOT tag >10bps), glossary section. Backup of v1 still in backups/goldstones-20260814-1634/.
