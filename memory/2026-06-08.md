# 2026-06-08

## Heartbeat health check

- 19:45 CST heartbeat found `valr-perpetual-grid-bot@sol.service` and `valr-perpetual-grid-bot@eth.service` inactive after 38 restart attempts each.
- Grid bot logs show startup failure: computed quantity is `0`, below minimum size (`0.01` SOL, `0.001` ETH). Read-only VALR balance check confirmed grid subaccounts have effectively no available trading capital:
  - SOL grid: USDC total `0.45281172` locked as `borrowReserved`; USDT total `-0.27049212`, borrowed `0.27049212`; available USDT `0`.
  - ETH grid: USDC available/total `0.00745648`; USDT `0`.
- `valr-cm-spot.service` was inactive and disabled; no matching `bot.py` process running. Latest app log activity ended around 18:56 CST.
- `cm-bot-v2.service` was active, but recent logs show it repeatedly pausing because CM1 balance is about `$1.19` and CM2 about `$20.03`.
- Log sanity looked okay: no single `.log` over 1GB, quarantine `4.0K`, `cm-bot-logrotate.timer` active with last run at 19:04 CST and next at 20:00 CST.

## Balance net-flow tracker

- Added daily net-flow accounting to `bots/balance-report`: `balance_report.py` now records daily account balances plus trade fees, maker rewards, funding net, borrow interest, and account flows into `net_flows.json`.
- Replaced monthly report logic so `monthly_report.py` reconciles from daily rows instead of estimating spot fees at a flat rate.
- First no-send run for 2026-06-08 wrote a corrected row:
  - Opening from 2026-06-07 snapshot: `$714.61`
  - Closing balance: `$5,714.50` USDC-ref
  - External/account flow: `+$5,000.00`
  - Trade fees paid: `$9.41`
  - Funding net: `+$0.09`
  - Net fee/funding effect: `-$9.31`
  - Inventory value movement: `+$9.20`
  - Reconciliation check: OK
- Important accounting nuance: VALR labels the `$5,000` top-up as `INTERNAL_TRANSFER`; the tracker treats internal-transfer credits/debits as account flow so transfers inside tracked subaccounts net to zero, while one-sided funding into the tracked account set is captured.
- Blake clarified the preferred fee model: `taker notional × 0.02%`. Updated the daily tracker to store `taker_notional_ref` and derive `trade_fees_ref` from that formula instead of relying directly on VALR `feeValue`.
- Added `bots/balance-report/historical_log_reconcile.py` for historical examples from local logs.
- May 2026 worked example used 2026-05-05 → 2026-05-31 because May 1–4 balance snapshots only included Grid Bot 1:
  - Opening: `$2,878.15`, closing: `$930.25`, balance movement: `-$1,947.90`
  - Exact spot taker notional from timestamped PRINT lines: `$944,018.77` across `417,867` prints
  - Futures taker notional is prorated from rotated logs because futures files lack per-line timestamps: `~$3,578,850.51` across `~548,064` taker fills
  - Total taker notional: `$4,522,869.28`; estimated fees at 0.02%: `$904.57`
  - Implied inventory value movement: `-$1,043.33`
  - Reconcile: opening - fees + inventory movement = closing

## Wash account funding check

- Blake funded the wash trading accounts and asked for a health check.
- Spot wash:
  - `valr-cm-spot.service` was inactive/dead and disabled.
  - Found and fixed a logging-format crash in `bots/cm-bot-spot-py/src/cycle.py` on the external-fill warning path.
  - Verified with `python3 -m py_compile bots/cm-bot-spot-py/src/cycle.py bots/cm-bot-spot-py/bot.py` and `python3 -m pytest -q` from `bots/cm-bot-spot-py` (`28 passed`).
  - Enabled/started `valr-cm-spot.service`; it connected to CMS1/CMS2 websockets, cleared one stale CMS2 order, and resumed fresh spot wash prints.
  - Approx live balances after restart: CMS1 `$552` USDC-ref, CMS2 `$535` USDC-ref; open orders CMS1/CMS2 `0`.
- Futures wash:
  - `cm-bot-v2.service` stayed active and resumed fresh internal fills with funded balances around CM1 `$201` and CM2 `$220`.
  - Ran the existing `bots/cm-bot-v2/scripts/net_positions.py` during the check; it placed paired offset orders and finished with open orders CM1/CM2 `0`.
  - Residual net futures exposure still needs watching: roughly SOL `+$165` long and XRP `+$174` long, smaller BTC/AVAX/DOGE drift, ETH nearly flat. Logs show corrective mode active, so do not call it perfectly neutral yet.

## Spot wash quote-bucket split

- Blake approved reworking the unified spot wash bot into separate ZAR, USDT, and USDC services, including creating/renaming subaccounts and moving/procuring inventory, with the explicit restriction: do not move inventory from the main account without asking.
- Refactored `bots/cm-bot-spot-py` so the same codebase can run separate instances with per-config account IDs/names and instance log names.
- Added split configs:
  - `config-zar.json`: 9 ZAR pairs, rate limit 25/s
  - `config-usdt.json`: 13 USDT pairs, rate limit 35/s
  - `config-usdc.json`: EURCUSDC only, rate limit 10/s
- Added templated systemd unit `valr-cm-spot@.service`.
- Created 6 proprietary VALR subaccounts using the primary CMSpot API key:
  - `CMSZAR1` `1513524239074144256`
  - `CMSZAR2` `1513524288865144832`
  - `CMSUSDT1` `1513524297399840768`
  - `CMSUSDT2` `1513524305939443712`
  - `CMSUSDC1` `1513524314495823872`
  - `CMSUSDC2` `1513524323040333824`
- Stopped/disabled old unified `valr-cm-spot.service`; no open orders on old CMS1/CMS2 before migration.
- Moved only existing wash-account inventory from CMS1/CMS2 into the new subaccounts. Did not move any main-account inventory.
- Seeded missing quote/base inventory with market orders inside the new wash buckets:
  - Sold 90 USDT to ZAR in each ZAR account via `USDTZAR`.
  - Bought small ETH and BTC seeds in each ZAR account via `ETHZAR` and `BTCZAR`.
  - Bought EURC seeds in each USDC account via `EURCUSDC`.
- Deployed and enabled:
  - `valr-cm-spot@zar.service`
  - `valr-cm-spot@usdt.service`
  - `valr-cm-spot@usdc.service`
- Verification:
  - `python3 -m pytest -q` in `bots/cm-bot-spot-py`: `28 passed`.
  - Dry-runs with new account IDs started all three quote buckets.
  - Live services active/enabled; old unified service inactive/disabled.
  - Open orders across all six new spot subaccounts: `0`.
  - Fresh split logs show ZAR prints on LINKZAR/ETHZAR/XRPZAR/BNBZAR/XAUTZAR/AVAXZAR, USDT prints on HOODXUSDT/CRCLXUSDT/MSTRXUSDT, and USDC print on EURCUSDC.
  - No fresh stderr or traceback in split service logs.
- Caveat: some USDT pairs still skip naturally due thin base inventory. Old CMS1/CMS2 still hold leftover balances that can be tidied after the split services prove stable.
- Follow-up after Blake asked about old CMS residuals:
  - Confirmed split configs cover all 23 pairs from old `config.json`; missing pairs `0`.
  - Drained remaining old CMS1/CMS2 wash inventory to the new quote buckets. Old CMS1 and CMS2 now show `$0.00` USDC-ref and `0` open orders.
  - Mapping used: ZAR-only bases to `CMSZAR1/2`, USDT quote/bases to `CMSUSDT1/2`, EURC/USDC to `CMSUSDC1/2`, and leftover XAUT split between ZAR and USDT buckets.
  - Did a second USDT-only market-order seed pass for under-seeded bases (`NVDAX`, `COINX`, `TRUMP`, `TSLAX`, `BITGOLD`, `VALR10`, `HOODX`, `MSTRX`) using existing `CMSUSDT1/2` USDT only.
  - After seeding, USDT logs showed fresh prints on NVDAXUSDT, COINXUSDT, BITGOLDUSDT, XAUTUSDT, JUPUSDT, MSTRXUSDT, HOODXUSDT, VALR10USDT, and CRCLXUSDT.
  - Open orders across old CMS1/CMS2 plus all six new subaccounts remained `0`.
- Blake then asked to delete old `CMS1`/`CMS2`.
  - Verified both old subaccounts had `$0.00` balance and `0` open orders.
  - Called documented `DELETE /v1/account/subaccount` for both old IDs.
  - VALR returned `204` but initially still listed them; after a short delay and a rename/archive attempt, final verification returned `Subaccount not found` for both:
    - old `CMS1` `1483815480334401536`
    - old `CMS2` `1483815498551132160`
  - Disabled all 23 pairs in legacy unified `bots/cm-bot-spot-py/config.json` as a guardrail. Live source of truth is now only `config-zar.json`, `config-usdt.json`, and `config-usdc.json` with `valr-cm-spot@zar/usdt/usdc.service`.
  - Post-check: split services active, old `valr-cm-spot.service` inactive, tests `28 passed`.
