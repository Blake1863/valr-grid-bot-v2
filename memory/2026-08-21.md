# 2026-08-21

## Cron/scheduler cleanup (requested ~13:47)
User: never sent the "empty messages" — they were heartbeat polls misread as user messages, plus leaked exec-failure warnings. Asked to clean up crons again + stop leaking exec failures.

### Findings
- OpenClaw cron: 0 jobs (already clean).
- **bot-health-monitor.py (crontab 06:00/18:00) was auto-restarting dead wash bots** via `systemctl --user restart` — the resurrection risk HEARTBEAT.md warned about. Killed.
- `cm-drain-eurc-usdc.timer` still active+enabled, moving funds 4-hourly (next trigger was 15:15) → stopped + disabled.
- Crontab leftovers removed: cm-bot-v2 internal_offset (*/4h), rebalance_usdt (hourly), daily_fund_check (22:00), bybit monitor.py (*/5min, "No open positions" since May), grid-backtester (weekly, cron.log empty), `/tmp/valr-grid-autocommit` (target file gone).
- Kept: balance_report.py (05:00 UTC), monthly_report.py (1st, 05:30), cm-bot-logrotate.timer.
- No stray bot processes running; units all disabled/masked.
- Backup: `backups/crontab-2026-08-21-pre-cleanup.bak`.

### Leak fixes
- `agents.defaults.heartbeat.suppressToolErrorWarnings` = PROTECTED config path, gateway config.patch refused. Did NOT bypass.
- Behavioral fix instead: rewrote HEARTBEAT.md — all check commands end `|| true` (exec never fails → no warning payload), explicit ground rule: heartbeat poll ≠ user message, reply HEARTBEAT_OK when nothing needs attention.
- If warnings ever recur: user can set that flag manually (openclaw config).

## Balance report retired (~13:59)
User: balance report never delivered anyway → end it. Removed entire crontab (balance_report.py + monthly_report.py). Backup: `backups/crontab-2026-08-21-final.bak`. Box now has ZERO crontab entries; only systemd timer left is cm-bot-logrotate (log hygiene, keep).
